# CSS: evitar versao antiga apos deploy (Hostinger / LiteSpeed / CDN)
<IfModule mod_headers.c>
    <FilesMatch "\.css$">
        Header set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
        Header set Pragma "no-cache"
        Header unset ETag
    </FilesMatch>
</IfModule>
